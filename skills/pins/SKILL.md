---
description: List all pinned Claude Code sessions with their names, projects, and pin dates.
disable-model-invocation: true
allowed-tools: Bash(cpin:*)
---

!`cpin list --plain`

The list of pinned sessions is above. Reply with that list unchanged (or a one-line note if it was empty). Do not summarize or interpret.

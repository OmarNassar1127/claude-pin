{
  "name": "claude-pin",
  "description": "Pin Claude Code sessions so they survive the resume window. Adds /claude-pin:pin, /claude-pin:unpin, /claude-pin:pins, /claude-pin:note and the cpin CLI for a picker that resumes pinned sessions directly by id.",
  "version": "0.6.0",
  "author": {
    "name": "Omar Nassar"
  }
}
